schema {
  query: Query
}

type Query {
  getProfile(username: String!): Profile @aws_cognito_user_pools @aws_api_key
}

type Profile {
  username: String!
  displayName: String
  bio: String
  website: String
}
